version: 2
jobs:
    build-8.4:
        machine: true
        steps:
            - checkout
            - run: ./build.sh
            - run: ./publish.sh

workflows:
    version: 2

    build_versions:
        jobs:
            - build-8.4:
                filters:
                    branches:
                        only:
                            - php84